# AGENTS.md

## 专业角色与项目目标

1. 你是一位精通叶轮机械（压气机、涡轮）数值仿真的资深工程师。
2. 本项目聚焦于“从 `.geomTurbo` 文件自动生成可用于 CFD 计算的网格”功能开发。
3. 所有工作记录、说明文档和面向用户的输出均使用中文。

## 当前主实现

当前主实现位于项目根目录，并保持扁平、聚焦的结构：

```text
mesh.py
geomturbo.py
autogrid.py
quality.py
geometries/
tests/
```

- 未经用户明确要求，不重新引入 JSON 配置、包级子目录。
- 生成产物统一放在 `runs/`，其中内容均视为可丢弃运行产物。
- `.geomTurbo` 输入样例统一放在 `geometries/`。
- 项目设计、质量准则和网格控制项调研文档统一放在 `docs/`。

## 历史归档

`archive/` 只保存 `v0`～`v6` 历史快照，默认必须完全忽略：

- 不读取、不搜索、不修改其中内容；
- 不从其中导入代码或执行测试；
- 不把其中实现作为当前需求的默认依据；
- 只有用户明确要求查看或处理历史版本时，才允许进入该目录。

## 基本规则

1. **运行环境**：项目仅依赖 Python 标准库，无需安装第三方包，使用系统任意 Python ≥3.7 即可运行。

2. **修改日志**：任何代码修改、重构后，都必须用中文记录到docs目录下的`DevLog.md` 文件中。

3. **Git 提交规范**：commit 消息必须遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范且使用中文。

4. **Git 工作方式**：未经用户明确要求，不使用 Git worktree，也不新建分支进行提交；默认在当前工作目录和当前分支上完成工作。

5. **最小改动原则**：所有代码修改均遵循最小改动原则，仅改动实现当前需求所必需的代码，不触碰无关逻辑，不影响已有功能。

# 审查问题修复与验收记录

本轮实现审查报告 R01～R16，保留 Schema v4、CLI 标准库依赖及各类运行状态的独立语义。没有访问 `archive/`，没有新建分支或提交，保留工作区原有文档删除。

## 修复对应

| 编号 | 实现结果 |
|---|---|
| R01 | 最终参数核验使用生成后回读，标记 `basis=post_generation`；应用证据仍保留，中文报告标明新旧核验依据。 |
| R02 | 已知质量字段先识别再解析，全局、逐叶排非法统计返回 UNKNOWN，错误路径保留，非有限/不可解析数值输出 null。质量规则版本为 2，阈值不变。 |
| R03 | 按心跳间隔回收其他 Worker 的过期运行，新回收任务加入后处理，不重置活动任务及后处理。 |
| R04 | 后处理失败事务同步预览终态，manifest 明确返回 FAILED，正常冻结不再被无效等待阻塞。迁移 0003 修正既有异常记录并追加恢复事件。 |
| R05 | SQLite 只读入口编码文件 URI，源/备份路径中的 # 与 % 不再指向错误库。 |
| R06 | CGNS 同块嵌套及跨边界扫描修正，闭合后不保留外部正文；原 8 MiB 复现只保留 695 字节质量片段。 |
| R07 | 目标点数暂时停用，CLI 两入口静态拒绝，Web 生效快照和重试拒绝；目录及历史记录保留，旧继承值可清除；campaign 跳过并明确记录停用。 |
| R08 | IGG 候选路径在切换运行目录前绝对化。 |
| R09 | 输入哈希/大小来自实际输入副本，源码/Git/注册表来源在启动前采集。 |
| R10 | campaign 每次实际执行采用新 attempt 目录，恢复与串行重试不覆盖旧结果。 |
| R11 | 详情和事件根据自身/后处理状态更新，终态补取；手动刷新当前面板。真实浏览器发现的控制目录旧缓存也通过父状态查询键修复。 |
| R12 | 经验草稿固定基准版本，远端变化不替换编辑基准；冲突保留草稿，成功采用响应，撤销恢复最新远端内容。 |
| R13 | 小质量值采用科学计数法，不显示为零。 |
| R14 | null、空白、非有限值不参与差值，真实零仍可比较。 |
| R15 | mutation 401 与查询统一恢复认证，已打开工作台隐藏且保留组件/草稿，不自动重放写操作。 |
| R16 | 安装及环境模板明确密码命令 PYTHONPATH，区分测试依赖和 CLI 标准库运行依赖。 |

同时修复原始数值编辑态的 dirty/撤销行为、完成阶段误记及过时文档。停用目标点数是本轮已确认的功能边界，不代表已实现目标点数生成算法。

## 验证结果

- 内核全套：93 项通过，另有 65 个 subtests。
- 后端：全套 113 项通过、4 项 POSIX 场景跳过；一个引用旧内部函数名的预览夹具修正后单项通过，合计 114 项通过。
- 前端：全套 72 项通过；随后真实浏览器发现控制目录缓存遗漏，新增 1 项回归并通过控制编辑器全部 12 项，累计覆盖 73 项。最终前端生产构建通过。
- 各根因定向回归覆盖输入变更、生成后覆盖、特殊路径备份、数据库迁移、Worker 回收、后处理异常、草稿冲突、认证恢复、CGNS 边界及数值显示；不重复执行已通过且未变化的用例。
- 安装入口：以 Python `-S` 禁用 editable `.pth`，仅保留普通已安装依赖并按文档设置 PYTHONPATH，密码 CLI 模块可执行；不读取或修改实际账号密码。证据为 `clean-install-check.log`。

真实 AutoGrid 17.1 / Edge 浏览器验收：

| 运行 | 参数 | 生成/质量 | 点数 | 后处理/预览 |
|---|---|---|---:|---|
| Rotor37 baseline | 默认 | SUCCEEDED / PASS | 1,464,289 | COMPLETED / READY |
| 分支 #2 | optimization.steps=200 | SUCCEEDED / PASS | 1,464,289 | COMPLETED / READY |

分支参数生成后回读 200，核验 VERIFIED；浏览器保存中文经验为 v1、下载 CGNS 并核对登记哈希、冻结会话并刷新，满意节点和经验文本保持。验收会话为 `a05b459f-146f-4ae3-95df-5d1f40d0f7fa`，仅存在于隔离数据目录。

证据均在 `runs/implementation_20260912/`：`core-final.log`、`platform-final.log`、`backend/`、`ui-final.log`、`ui-build-final.log`、`e2e-result.json` 及 `.playwright-cli/`。本轮没有重跑完整 344 项控制矩阵，也没有扩展目标点数算法调研。

## 本地部署与恢复

已按根目录 `.env` 配置升级 `runs/platform-dev/mesh.sqlite3`：数据库 v2→v3，原 8 个成功运行保持。升级前确认无在途任务和后处理，备份工具登记 282 项产物，缺失/哈希不匹配均为 0；实际几何、产物、预览、日志及旧前端文件已复制到 `runs/implementation_20260912/deployment-backup/`，不以清单代替文件备份。

当前 API/Worker 已启动，地址为 `http://127.0.0.1:8000`，共享账号配置保持不变；这是本机启动，没有注册自启动任务。进程、备份路径与健康检查详见 `deployment-result.json`。隔离验收服务和测试浏览器已关闭。

回退时先停止当前服务，再恢复升级前数据库、实际文件和配套旧代码/前端；不要将旧代码直接连接 v3 数据库。备份位于 runs，请在清理运行产物前自行保留所需备份。
